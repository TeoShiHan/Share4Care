<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.share4care">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Share4Care">
        <activity
            android:name=".ee.PostDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_post_detail"
            android:theme="@style/Theme.Share4Care.NoActionBar" />
        <activity
            android:name=".ee.CreateEventActivity"
            android:exported="false" />
        <activity
            android:name=".ee.CreateActivity"
            android:exported="false"
            android:label="@string/title_activity_create"
            android:theme="@style/Theme.Share4Care.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>